# N高グループ文化祭実行委員会 クリエイティブ班 業務システム 仕様書

## 1. 概要

### 1.1. システム名
N高グループ文化祭実行委員会 クリエイティブ班 業務システム

### 1.2. 背景と目的
N高グループ文化祭実行委員会のクリエイティブ班は、文化祭で使用される多種多様な制作物（画像、映像、Webサイト、造形物など）を一手に担います。依頼の受付、担当者の割り当て、進捗管理、成果物の納品といった一連の業務は、案件数が多くなるにつれて複雑化し、管理が煩雑になるという課題がありました。

本システムは、この課題を解決するために開発された業務基幹システムです。Google Workspaceのサービス群（スプレッドシート, フォーム, Apps Script）を最大限に活用し、以下の目的を達成します。

- **一元管理**: 散在しがちな制作依頼や関連情報を一つのデータベース（スプレッドシート）に集約する。
- **自動化**: 依頼受付からフォルダ作成、タスクリストの生成、通知までの一連の定型業務を自動化し、手作業によるミスや負担を削減する。
- **可視化**: 各案件のステータス、担当者、納期、負荷状況をリアルタイムで可視化し、円滑なプロジェクト進行とリソースの適切な配分を支援する。

### 1.3. 対象利用者
- **クリエイティブ班の班員**: 制作業務の担当者、進捗管理者。
- **文化祭実行委員会のその他メンバー**: 制作依頼者。
- **システム管理者**: システムの保守・運用担当者。

## 2. システムアーキテクチャ

### 2.1. 構成図
本システムは、Googleの各種サービスを連携させたサーバーレスアーキテクチャで構築されています。

```
(依頼者)
   |
   v
[Googleフォーム] --(依頼提出)--> [Googleスプレッドシート "form"タブ]
   |                                        |
   |                                        v (onFormSubmitトリガー)
   |                                  [Google Apps Script]
   |                                        |
   +----------------------------------------+-----------------------------------------+
   | (1. データ処理)                        | (2. 環境構築)                           | (3. 通知)
   v                                        v                                         v
[Googleスプレッドシート]                  [Google Drive]                            [Slack]
- "works"タブにレコード追加                - 制作/納品フォルダ作成                    - 依頼受付をチャンネルに通知
- "projects"タブに案件情報更新             - 制作管理シート(コピー)作成
- "members"タブの担当数更新
   ^                                        ^
   | (データ同期)                           |
   +----------------------------------------+
   |
(クリエイティブ班員)
   |
   v
[制作管理シート (個別)] <--(双方向同期)--> [リソース管理シート "works"タブ]
```

### 2.2. 主要コンポーネント
- **Google スプレッドシート (リソース管理シート)**:
    - **役割**: システム全体のマスターデータベース。全ての制作依頼、案件、メンバー情報を集約します。
    - **特徴**: `works`, `projects`, `members`などの複数のタブ（シート）で構成され、それぞれがテーブルとして機能します。UIとしての役割も兼ね、班員はここから全体の状況を把握します。

- **Google スプレッドシート (制作管理シート)**:
    - **役割**: 依頼ごとに自動生成される個別の作業用シート。詳細なタスク管理や情報集約を行います。
    - **特徴**: `main`（依頼詳細）、`tasks`（タスクリスト）のタブを持ち、担当者は主にこのシートを編集して進捗を更新します。

- **Google フォーム**:
    - **役割**: 依頼受付のメインインターフェース。誰でも簡単に依頼を提出できます。
    - **特徴**: 提出された回答は、リソース管理シートの`form`タブに自動的に記録されます。

- **Google Apps Script**:
    - **役割**: システムの頭脳となるバックエンドロジック。
    - **特徴**: イベントドリブンで動作し、フォーム送信(`onFormSubmit`)やシート編集(`onEdit`)をトリガーとして、データ処理、ファイル操作、外部連携など、本システムのほぼ全ての自動化処理を実行します。

- **Google Drive**:
    - **役割**: 成果物と関連ドキュメントのストレージ。
    - **特徴**: Apps Scriptによって、依頼ごとに専用の「制作フォルダ」「納品フォルダ」が自動で生成され、体系的なファイル管理を実現します。

- **Slack**:
    - **役割**: コミュニケーションハブ。
    - **特徴**: Apps Scriptの`UrlFetchApp`サービスを介してSlackのWebhook URLにPOSTリクエストを送信し、新規依頼の受付などをリアルタイムで関係者に通知します。

## 3. 機能仕様

### 3.1. 制作依頼受付 (`flow_receptionRequest.gs`)
- **機能**: Googleフォームまたはスプレッドシートから提出された制作依頼をシステムに登録する。
- **フロー**:
    1.  **トリガー**: Googleフォームの送信(`onFormSubmit`)、または班員による手動実行。
    2.  **データ整形 (`formatFormResponse`)**: フォームの回答データを、`workInfo`（制作物情報）と`requestInfo`（依頼詳細）の2つのオブジェクトに整理する。
    3.  **案件特定/作成 (`findOrCreateProjectId`)**: `projects`シートを検索し、依頼された案件タイトルが既存であればその案件番号を取得。なければ新しい案件番号を採番し、`projects`シートに新しい行を追加する。
    4.  **制作番号採番 (`determineWorkId`)**: `works`シートの既存の制作番号から、一意の新しい制作番号を計算して採番する。
    5.  **作業環境構築 (`setupWorkEnvironment`)**:
        -   `createNewFolder`: Google Drive上に、命名規則に従った「制作フォルダ」と、その中に「素材フォルダ」「納品フォルダ」を作成する。
        -   `createWorkSheet`: `parameters`シートで指定されたテンプレートから「制作管理シート」をコピーし、命名規則に従ってリネームする。
    6.  **データ書き込み**:
        -   `writeResponseToSheet_resource`: 整形した依頼情報を「リソース管理シート」の`works`タブに書き込む。
        -   `writeResponseToSheet_work`: 新しく作成した「制作管理シート」の`main`タブと`tasks`タブに、依頼内容やタスクリストの初期状態を書き込む。
    7.  **通知 (`sendNotificationToSlack`)**: 依頼内容を整形し、Slackの指定されたチャンネルに通知する。依頼者のSlack IDは、`getSlackIdByEmail`関数で連絡先シートから取得する。
    8.  **システムコマンド処理 (`processSystemCommand`)**: 依頼フォームで特別なコマンドが入力された場合（例: `dontSendNotification`）、通知をスキップするなどの追加処理を行う。

### 3.2. データ同期 (`flow_syncSheet_*.gs`)
- **機能**: 「リソース管理シート」と個別の「制作管理シート」の間で、データの整合性を自動的に保つ。
- **トリガー**: `onEdit`イベント（シートのセルが編集されたとき）。

- **リソース管理 → 制作管理 (`syncSheet_resourceToWork`)**:
    - **ユースケース**: 管理者が`works`シートで担当者や納期を変更した場合。
    - **フロー**:
        1.  `works`シートでの編集を検知。
        2.  変更された行の「制作シートURL」を取得し、対象の「制作管理シート」を開く。
        3.  `works`シートの行データを取得し、「制作管理シート」の対応するセル（タイトル, 担当者リスト, 各種日時など）に値を上書きする。
        4.  担当者が変更された場合、`main`タブの担当者リストのチェックボックスの状態を更新する。

- **制作管理 → リソース管理 (`syncSheet_workToResource`)**:
    - **ユースケース**: 担当者が「制作管理シート」でタスクのステータスを変更したり、担当者を追加したりした場合。
    - **フロー**:
        1.  「制作管理シート」での編集を検知。
        2.  `main`タブの「管理番号」をキーとして、「リソース管理シート」の`works`タブから対応する行を特定する。
        3.  編集内容に応じて、`works`シートの対応するセルを更新する。
            -   担当者チェックボックスの変更 → `担当者`列のカンマ区切りの氏名リストを更新。
            -   `tasks`タブのステータス変更 → `ステータス`列と、対応する`○○開始日時`または`納品日時`を更新。

### 3.3. 案件情報の整合性維持 (`tab_resource_projects.gs`)
- **機能**: `works`シートと`projects`シート間で、案件番号と案件タイトルの整合性を保つ。
- **トリガー**: `onEdit`イベント。
- **フロー**:
    -   `works`シートで**案件番号**が変更された場合:
        1.  `projects`シートを参照し、新しい案件番号に対応する**案件タイトル**を取得。
        2.  `works`シートの案件タイトルを自動で更新する。
    -   `works`シートまたは`projects`シートで**案件タイトル**が変更された場合:
        1.  関連する全ての`works`シートの行の案件タイトルを、変更後のタイトルに一括で更新する。
        2.  もし変更後のタイトルが他の既存案件と重複する場合、ユーザーに確認ダイアログを表示し、案件番号を統合するか、変更をキャンセルするか選択させる。

## 4. データベーススキーマ (スプレッドシート)

### 4.1. リソース管理シート
#### `works`タブ
| ヘッダー名 | 説明 | データ型 | 例 |
| :--- | :--- | :--- | :--- |
| 管理番号 | `システム利用開始年-制作番号-案件番号`の形式。システム内の主キー。 | 文字列 | `25-0001-AA` |
| 案件番号 | `projects`タブと連携する外部キー。 | 文字列 | `AA` |
| 制作番号 | 依頼ごとに採番される一意の番号。 | 数値 | `1` |
| 制作タイトル | 制作物の名前。 | 文字列 | `マスコット紹介動画` |
| ステータス | 制作の進捗状況。 | 文字列 | `制作` |
| ジャンル | 制作物の形態（単一選択）。 | 文字列 | `動画` |
| 依頼受付開始日時 | 依頼を受け付けた日時。 | 日時 | `2025/09/01 10:00` |
| ... (その他日時) | 初回ヒアリング、制作開始、納品などの日時。 | 日時 | |
| 依頼者メアド | 依頼者のメールアドレス。 | 文字列 | `user@example.com` |
| 依頼班 | 依頼者の所属班（メールアドレスを外部キーとして、名簿DBから取得）。 | 文字列 | `ステージ班` |
| 依頼者 | 依頼者の所属班（メールアドレスを外部キーとして、名簿DBから取得）。 | 文字列 | `高尾真一` |
| 担当者 | 担当者の氏名（カンマ区切り）。 | 文字列 | `山田太郎, 鈴木花子` |
| 制作シート | 自動生成された制作管理シートへのリンク。 | URL | `https://docs.google.com/...` |
| ... (その他URL) | 制作フォルダ、納品フォルダなどへのリンク。 | URL | |

#### `projects`タブ
| ヘッダー名 | 説明 | データ型 | 例 |
| :--- | :--- | :--- | :--- |
| 案件番号 | 案件の一意なID。主キー。 | 文字列 | `AA` |
| 案件タイトル | 案件の名前。 | 文字列 | `オープニングステージ` |
| 未納数 | この案件に含まれる未納の制作物数。 | 数式 | `=COUNTIFS(...)` |

#### `members`タブ
| ヘッダー名 | 説明 | データ型 | 例 |
| :--- | :--- | :--- | :--- |
| Slack 表示名 | メンバーの氏名。 | 文字列 | `山田太郎` |
| 担当制作数 | 担当している制作物の数。 | 数式 | `=COUNTIF(...)` |
| E-mail | メンバーのメールアドレス。 | 文字列 | `member@example.com` |

#### `parameters`タブ
| ヘッダー名 | 説明 | データ型 | 例 |
| :--- | :--- | :--- | :--- |
| `contactSheet.id` | 連絡先シートのID。 | 文字列 | `1a2b3c...` |
| `workInfo.url.workSheet` | 制作管理シートのテンプレートID。 | 文字列 | `4d5e6f...` |

### 4.2. 制作管理シート
#### `main`タブ
依頼内容、デザイン要項、入稿規定、参考物など、依頼に関する詳細情報をテキストで記述するエリアと、担当者を選択するチェックボックスリストで構成される。

#### `tasks`タブ
| ヘッダー名 | 説明 | データ型 | 例 |
| :--- | :--- | :--- | :--- |
| タイトル | タスクの名称（例: `依頼受付`, `初回ヒアリング`）。 | 文字列 | `制作` |
| ステータス | `未着手`, `実行中`, `完了`のいずれか。 | 文字列 | `実行中` |
| 開始日時 | タスクを開始した日時。 | 日時 | `2025/09/02 14:00` |
| 終了日時 | タスクを完了した日時。 | 日時 | `2025/09/05 18:00` |

## 5. スクリプト構成 (`.gs`ファイル)

- **`global_triggers.gs`**:
    - `onOpenFunctions`: シートを開いたときにカスタムメニューを作成する。
    - `onEditFunctions`: シート編集時に各機能（データ同期、整合性維持など）を呼び出すハブとなる関数。
    - `onFormSubmitFunctions`: フォーム送信時に`receptionRequest`を呼び出す。
    - `createMenu`: `班員用メニュー`をUIに追加する。
- **`global_functions.gs`**:
    - `CONFIG`: シート名、ヘッダー名、ステータス名などの定数を一元管理するオブジェクト。
    - `getValueRanges`: 特定の値を持つセル範囲を検索する汎用関数。
    - `getColByHeaderName`: ヘッダー名を基に列番号を取得する。
    - `getSlackIdByEmail`: メールアドレスをキーに連絡先シートからSlack IDを取得する。
- **`flow_*.gs`**:
    - `flow_receptionRequest.gs`: 上記「機能仕様 3.1」で詳述した新規依頼受付のコアロジック。
    - `flow_syncSheet_resourceToWork.gs`: リソース管理シートから制作管理シートへのデータ同期ロジック。
    - `flow_syncSheet_workToResource.gs`: 制作管理シートからリソース管理シートへのデータ同期ロジック。
- **`tab_resource_*.gs`**:
    - `tab_resource_projects.gs`: `integrityProjIdAndTitle`など、案件情報の整合性維持に関する関数。
    - `tab_resource_work.gs`: `inputStatusChangedDatetime_resource`など、`works`タブの編集に関連するヘルパー関数。
    - `tab_resource_members.gs`: `openOrCloseFormResponse`など、依頼受付の可否をフォームに反映する関数。
- **`tab_work_*.gs`**:
    - `tab_work_main.gs`: `inputLastModifiedDate`など、制作管理シートの`main`タブに関連する関数。
    - `tab_work_tasks.gs`: `inputStatusChangedDatetime_work`など、`tasks`タブのステータス変更と日時入力を連動させる関数。

## 6. セットアップと設定

### 6.1. 初期設定 (`flow_setup.gs`)
- `setTriggers`: `onOpen`, `onEdit`, `onFormSubmit`のトリガーをプログラム的に設定する。初回セットアップ時に一度だけ実行する必要がある。

### 6.2. スクリプトプロパティ
Apps Scriptのプロジェクト設定で、以下のキーと値を設定する必要がある。これらはスクリプト内で`PropertiesService`を通じて参照される。
- `slackWorkflow_notifyRequest_WebReqestUrl`: Slack通知を送信するためのWebhook URL。
- `slackAdminEmail`: 依頼者のSlack IDが見つからない場合にメンションされる管理者（フォールバック用）。
- `sheetId_resource`: リソース管理シート自体のID。制作管理シートからリソース管理シートを参照するために使用。

### 6.3. マニフェストファイル (`appsscript.json`)
- `timeZone`: `Asia/Tokyo`に設定。
- `dependencies.enabledAdvancedServices`: Google Sheets API (`Sheets`) を有効化。フィルタービューの操作などで使用。
- `runtimeVersion`: `V8`ランタイムを使用。
